<h1>All Tasks</h1>
  <% @tasks.each do |t| %>
    <p><%=link_to t.title, task_path(t) %> . <%=link_to "Edit", edit_task_path(t) %></p>
  <% end %>
<%= link_to "Add task", tasks_new_path ,class: "btn btn-primary" %>
